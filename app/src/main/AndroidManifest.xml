<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gabrielascurra.productos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Controlador.Anadir"
            android:label="@string/titulo_cargar" />
        <activity
            android:name=".Controlador.Modificar"
            android:label="@string/titulo_modificar"></activity>
        <activity android:name=".Controlador.Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controlador.HistorialItemsActivity"></activity>
    </application>

</manifest>